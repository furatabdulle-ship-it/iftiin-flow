
import { SlideData, SlideType } from './types';
import { translations } from './utils/translations';

// Helper to get slides based on current translation object
export const getPosts = (lang: 'EN' | 'DE') => {
  return [];
};
